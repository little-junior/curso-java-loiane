package com.liljon.curso_java.aula13;

public class OperadoresLogicos {
    public static void main(String[] args) {

        /*
        && = and (curto circuito)
        || = or (curto circuito)
        ^ = xor
        !(antes) = not
         */
    }
}
